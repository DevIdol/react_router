import styles from "./Expenses.module.css"
const Expenses = () => {
    return(
        <main className={styles.expenses}>
            <h2>Expenses</h2>
        </main>
    );
}

export default Expenses;